/* exported head */

/*
- create storage for output
- look at array
- locate the first item of the array
- push it to the output
- return output
*/

function head(array) {
  var output = '';
  output = array[0];
  return output;
}
