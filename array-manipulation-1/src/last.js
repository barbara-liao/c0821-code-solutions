/* exported last */

/*
- create storage for output
- look at the array
- determine the length
- find the last element of the array
- assign the last element to the output
- return the output
*/

function last(array) {
  var output = '';
  output = array[array.length - 1];
  return output;
}
